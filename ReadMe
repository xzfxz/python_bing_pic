#该工程使用python3 以及beautilfulsoap4实现的图片下载

#下载 beautifulsoup 的tar包
https://www.crummy.com/software/BeautifulSoup/#Download
# 安装 beautifulsoup
# 解压如上的tar包，进入该目录，执行如下语句
sudo python3 setup.py install

#安装的过程中需要如下的依赖的setuptools
#setuptools github homePage
https://github.com/pypa/setuptools

# 安装setuptools需要使用pip工具，因此首先安装pip

# install pip，通过git clone https://github.com/pypa/setuptools.git 获取如上的代码，进入该目录，执行如下语句
sudo python3 get-pip.py


# install setuptools
sudo pip install -U pip setuptools

#当然也可以使用pip在线安装beautifulsoup，方法如下：
 easy_install beautifulsoup4
 pip install beautifulsoup4


# 运行程序的方式
1、安装系统依赖 beautifulsoup4
2、python3 main_bing_big_pic.py
